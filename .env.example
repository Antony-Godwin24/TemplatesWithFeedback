Example .env for payload-next-minimal
Copy this to .env and fill in your values. Do NOT commit secrets to source control.

Mongo connection (Atlas) - keep the entire string in quotes when using cmd.exe or place it in a .env
Example (replace <PASSWORD> and username if needed):
DATABASE_URI="mongodb+srv://antonygodwins:<PASSWORD>@cluster0.6xte7fw.mongodb.net/payload-db?retryWrites=true&w=majority"

Or use a local MongoDB connection:
DATABASE_URI=mongodb://localhost:27017/payload-minimal

Secret for Payload admin sessions
PAYLOAD_SECRET=some-secure-secret
